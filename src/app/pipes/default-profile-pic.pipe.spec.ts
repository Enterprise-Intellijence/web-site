import { DefaultProfilePicPipe } from './default-profile-pic.pipe';

describe('DefaultProfilePicPipe', () => {
  it('create an instance', () => {
    const pipe = new DefaultProfilePicPipe();
    expect(pipe).toBeTruthy();
  });
});

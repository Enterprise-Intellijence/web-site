import { DefaultProductPicPipe } from "./default-product-pic.pipe";

describe("DefaultProductPicPipe", () => {
    it("create an instance", () => {
        const pipe = new DefaultProductPicPipe();
        expect(pipe).toBeTruthy();
    });
    });